# config.py

# CSS_SELECTOR = "[class^='product-content']" >>>> "CSS_SELECTOR = "a" , is it possible to use a genral selector?"
#                                                   like a  HTML tag selector like "a" or "div" to select all links or divs on the page?


REQUIRED_KEYS = [
    "productName",
    "productLink",
]
